import { NozeroDirective } from './nozero.directive';

describe('NozeroDirective', () => {
  it('should create an instance', () => {
    const directive = new NozeroDirective();
    expect(directive).toBeTruthy();
  });
});
